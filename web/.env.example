# Address of the deployed RevokeRegistry contract on Monad mainnet (chain 143).
# Without this the dashboard still scans and revokes normally — only the on-chain
# cleanup proof is disabled, and the UI says so rather than pretending otherwise.
NEXT_PUBLIC_REGISTRY_ADDRESS=
